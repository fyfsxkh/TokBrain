# TokBrain 开源风险报告

审计日期：2026-07-28
适用范围：当前源代码仓库，不包括本机 `data/`、`logs/`、`backups/`、虚拟环境和媒体文件。
说明：本文是工程与开源合规风险判断，不构成法律意见。

## 结论

当前代码可以作为带有明确风险披露的实验性、本地工具公开，但不能表述为“已获抖音授权”
或“完全合规、无风险”。

最主要的剩余风险不是 MIT 许可证，而是项目通过 F2 使用非官方平台请求机制。抖音现行
[用户服务协议](https://www.douyin.com/agreements/?id=6773906068725565448)
明确限制未经授权的爬虫、自动化工具、模拟下载和反向工程。作者开启“允许下载”只能作为
缩小媒体处理范围的信号，不能授权第三方使用非官方接口，也不当然授予转载、商用、训练
模型或再分发作品的权利。

若目标是把平台风险降到最低，最稳妥的发布形态仍是：公开 RAG、字幕、ASR、本地文件和
知识库核心，移除 F2/抖音解析模块，改为用户上传本地文件或接入经过审批的官方开放平台
接口。保留当前 F2 模块开源属于“知情接受较高平台合同风险”的发布选择。

## 风险矩阵

| 风险 | 等级 | 当前控制 | 剩余问题 |
|---|---|---|---|
| 平台协议与反向工程 | 高 | 用户主动提交、低频、串行、熔断、无验证码绕过 | F2 仍是非官方访问路径；开源会扩大传播和可复制性 |
| 作品著作权与人格权 | 中高 | 仅明确允许下载时处理完整媒体；其他情况不下载视频 | “允许下载”不等于允许转载、商用、模型训练或公开传播 |
| Cookie、密钥和个人数据 | 中 | 本地监听、DPAPI 加密、运行数据全部忽略 | 用户仍可能手工提交数据库、日志、截图或真实 Cookie |
| 滥用与派生项目 | 中 | 固定限额、全局网络锁、拒绝验证码绕过 | 开源后他人可删除限制；许可证和免责声明无法技术阻止 |
| 依赖与供应链 | 低到中 | 固定 Python 版本、npm 锁文件、隔离 F2、CI 与漏洞扫描 | F2 和平台响应结构可能随时变化；依赖需持续更新 |
| 开源许可证 | 低 | 根项目 MIT；保留 bilibili-rag MIT 通知；注明 F2 Apache-2.0 | 发布二进制时仍需附带所有被打包依赖的许可证和通知 |
| 商标与项目表述 | 低到中 | TokBrain 独立命名、非关联声明 | 不应使用抖音 Logo，也不应暗示官方合作、授权或认可 |

## 已落实的技术边界

- 只处理用户主动提交的单作品链接，不扫描账号、收藏夹或推荐流。
- 平台请求全局串行；设置每日上限、随机冷却和风险熔断。
- 不提供自动登录、浏览器 Cookie 抽取、验证码识别或绕过功能。
- 只有明确检测到作者允许下载时才进入完整视频处理。
- 禁止下载或权限未知时，不访问完整视频地址；只尝试字幕、独立音频或基础信息。
- 下载地址执行 HTTPS、域名、DNS、私网地址、重定向、内容类型和大小检查。
- 百炼 ASR 转写结果只接受阿里云 HTTPS 公网地址，并限制重定向和 10 MB 响应大小。
- 服务默认只监听 `127.0.0.1`，限制 Host、Origin 和 CORS。
- Cookie 与模型密钥使用 Windows DPAPI 加密，响应和日志不回显秘密。
- F2 源包隔离安装到不入库的 `.vendor/`；其旧元数据锁定的易受攻击依赖由审计后的兼容版本替代。

## 本次发布审计结果

- Git 发布候选：123 个文件，约 1.86 MB。
- `data/`、`logs/`、`backups/`、`.vendor/`、虚拟环境、Node 依赖和构建缓存均已确认被忽略。
- 发布候选秘密扫描：未发现真实 API Key、Cookie、私钥或数据库。
- 后端：96 项测试通过。
- 前端：29 项测试、ESLint 和 Next.js 生产构建通过。
- `pip-audit -r requirements.txt` 以及仅针对 F2 包本身的 OSV 审计：截至审计日期
  未发现已知漏洞；F2 不会安装的旧依赖固定项不计入实际运行环境。
- `npm audit`：截至审计日期未发现已知漏洞。

## 发布前硬性检查

- 不提交 `.env`、`data/`、`logs/`、`backups/`、数据库、媒体、Cookie 或 API Key。
- 运行 `scripts/prepublish_check.ps1`，确认发布候选文件和秘密扫描均通过。
- 运行后端测试、前端测试、Lint、生产构建和依赖漏洞扫描。
- GitHub 仓库说明中使用“非官方、本地、实验性工具”，不得声称获得平台授权。
- 启用 GitHub Secret Scanning、Dependabot 和 Private Vulnerability Reporting。
- 不发布抓取到的作品样本；测试只使用人工构造的离线 fixture。
- 若公开打包后的可执行文件或安装包，重新生成完整第三方许可证清单。

## 建议的发布范围

建议公开：

- `app/`、`frontend/`、`tests/`、`scripts/`
- `README.md`、`操作说明书.md`
- `LICENSE`、`THIRD_PARTY_NOTICES.md`
- `SECURITY.md`、`CONTRIBUTING.md`
- `.env.example`、依赖锁文件与 CI 配置

禁止公开：

- `data/`、`logs/`、`backups/`
- `.env`、真实 Cookie、模型密钥、账单密钥
- 下载的视频、音频、字幕、关键帧、知识库导出和用户数据库
- 本地虚拟环境、构建缓存、调试配置和研究压缩包
