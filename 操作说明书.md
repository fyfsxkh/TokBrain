# TokBrain v1.0.0 操作说明书

本说明书适用于正式版 **v1.0.0**（数据库 **schema v9**、**API contract 7**）。
产品版本、数据库结构版本与 API contract 是三个独立编号。

## 启动软件

- 首次使用双击根目录的 `安装.cmd` 安装依赖；它只为本次安装进程临时绕过
  PowerShell 执行策略，不会永久修改系统设置。
- `安装.cmd` 只安装项目依赖。请先安装 64 位 Python 3.12、Node.js 22 或更高版本
  （随附 npm 10 或更高版本）；Python 安装界面必须勾选 `Add python.exe to PATH`。缺少系统运行环境时，
  安装窗口会显示下载地址和修复步骤。
- 需要在终端查看完整过程时，也可以运行 `.\setup.cmd`。
- 日常启动双击根目录的 `启动.cmd`；重新启动双击 `重启.cmd`。
- 服务就绪后会自动打开 `http://127.0.0.1:3000`。若没有弹出网页，可手动在浏览器访问该地址。
- `安装.cmd` 每次在临时目录重建并验证干净的 Python 环境，执行 `pip check`、核验 F2
  来源，再运行 `npm ci` 和 Next.js 生产构建；旧环境只在全部成功后才被替换。
- 前端固定监听本机 3000 端口；后端读取 `.env` 的 `APP_HOST` / `APP_PORT`，默认
  `127.0.0.1:8000`。修改后必须重新运行 `安装.cmd` 更新前端构建。
- 启动、停止和重启共用 `data/runtime.json`：启动会在确认相关端口空闲后自动清理失效状态；
  停止对新版状态执行完整进程身份校验，对旧启动器状态则同时校验记录 PID、监听端口、
  API contract 和 TokBrain 页面特征。任一项不匹配都会保留状态并拒绝终止，不会按端口
  盲目关闭其他软件；重启只会在相同停止流程成功后继续启动。
- 中文项目目录下的状态与构建 JSON 固定按 UTF-8 读取。重启失败时窗口会保留，并将停止、
  启动阶段的完整输出写入 `data/logs/restart-control.log`。
- 启动失败时不要关闭提示窗口，并检查 `data/logs/backend-error.log` 与
  `data/logs/frontend-error.log`。

## 1. 工作方式

TokBrain 只处理用户主动提交的公开作品链接或本地文件。导入页提供“链接预检”、
“本地视频”和“外部视频数据包”三个入口：

1. 首次使用时打开“设置”。保存百炼 API Key；如匿名链接解析不稳定，再保存一次可选
   F2 Cookie。Cookie 会由 Windows DPAPI 加密并在后续导入中复用，无需每次重新输入。
2. 打开“导入 → 链接导入”，粘贴一条或多条分享链接；也可以直接粘贴包含描述文字的
   整段分享文案。
3. 点击“开始预检”。软件会创建持久化批次，并逐条显示解析、重复、受限或失败状态。
4. 对成功结果选择收藏夹，点击“确认加入待入库”。链接预检不会自动确认，也不会自动
   创建 AI 任务。
5. 需要正式处理时，再点击“入库”或到“知识库 → 待入库”发起处理；此时才会按权限、
   媒体时长、Token 和费用预算执行后续流程。

日常导入不需要运行 F2 CLI，也不需要制作 manifest、生成外部导入令牌或执行推送脚本。
这些步骤只面向需要程序化接入的高级外部工具。

本地视频不需要链接解析：选择文件、确认权利声明并上传成功后，仍先进入待确认区；
确认加入待入库后，只有用户点击“入库”才会执行 ASR、关键帧、OCR、总结和向量化。

已经用 F2 或其他工具取得一批视频时，不需要复制链接或制作推送脚本：打开“外部视频
数据包”，选择整个下载文件夹或 ZIP，确认权利后点击“上传并自动检测”。检测完成的结果
会进入同一个待确认区，后续仍是“确认加入待入库 → 主动入库”。

已完成的链接导入结果会在“导入”与其他页面之间切换时继续保留。无需继续使用的批次结果
可以从导入页清理；已经进入知识库的作品仍由知识库单独管理，不会因为清理导入结果而被
连带永久删除。

导入页顶部的风险提示在所有 13 套主题中都会保持红色警示语义。请仅处理本人有权访问和处理的公开内容。

## 2. 本地视频与批量输入

### 本地视频

1. 打开“导入 → 本地视频”，选择文件或拖入虚线区域。
2. 一次最多选择 10 个视频，一文件创建一条作品；标题默认使用不带扩展名的文件名。
3. 选择本批默认收藏夹，并勾选“我确认有权处理这些文件”。
4. 开始上传。客户端同时最多上传 2 个文件，每个文件独立显示校验、上传、重复或失败
   状态；某个文件失败不会撤销其他成功项。
5. 上传后可逐条修改标题、描述和收藏夹。成功项默认勾选，点击“确认加入待入库”。
6. 需要立即处理时再点击“入库”；否则稍后到“知识库 → 待入库”处理。

页面刷新后会恢复最近的本地导入批次：已经上传成功的条目可以继续确认；浏览器无法保留
尚未完成的文件句柄，因此未完成条目需要重新选择对应视频后续传。

本地导入的视频必须包含可识别的视频轨，单个最大 1 GB，且不能超过设置中的作品时长
上限。TokBrain 根据真实文件内容和 ffprobe 结果校验，不信任扩展名或客户端报告的时长。
同一视频以完整 SHA-256 识别，重复导入不会创建多个本地作品。

本地视频导入不会调用 F2，也不消耗每日链接解析额度；上传和确认本身不调用模型。
正式入库仍会占用媒体时长、AI Token 和费用预算。

### 链接批量输入

- 每批最多接受 10 个去重后的链接，超出部分不会入队，页面会显示拒绝数量。
- 支持短分享链接、`/video/` 和 `/note/` 公开链接。
- 链接末尾的中文标点会自动清理。
- 查询参数不会保存；同一规范化链接或作品 ID 在批内只处理一次。
- 不支持的域名、非 HTTPS 地址和无效文本会显示独立错误。
- 链接提交只进行预检；收藏夹在预检成功后逐条选择，并由用户手工确认。
- 保存过的可选 F2 Cookie 会自动复用；导入页不会再次索要 Cookie。

### 外部视频数据包（文件夹或 ZIP）

1. 打开“导入 → 外部视频数据包”，选择“文件夹”或“ZIP”。
2. 文件夹模式会选中其中受支持的视频和元数据；ZIP 模式由后端安全解包。每批最多
   100 个视频、单视频 1 GB、总大小 20 GB。
3. 后端按顺序识别：清单中的 `video_file`、F2 的 `douyin_videos.db`、同名 JSON/CSV、
   文件名中的抖音作品 ID。匹配不够确定时不会猜测，而是按本地视频导入，标题默认取文件名。
4. 上传并发固定为 2。刷新网页后，后端检测会继续；未传完的文件需要重新选择同一个
   文件夹或 ZIP，页面会匹配路径与大小后续传。
5. 检测完成后逐条检查标题和收藏夹，点击“确认加入待入库”；不会自动启动 AI。

ZIP 不接受加密包、嵌套 ZIP、符号链接、绝对路径、`..` 路径、路径碰撞或异常压缩比。
该入口不接受远程视频 URL、任意本机路径、纯元数据或图文作品。数据包检测不访问 F2，
也不消耗每日链接额度。

## 3. 进度与中断

进度区显示总数、已完成、可入库、需补件、重复、失败、取消、剩余数量、百分比和最多
3 个 worker 的当前条目。

点击“中断解析”后：

- 页面进入“正在安全停止”。
- 尚未开始的条目标记为 `cancelled_by_user`。
- 正在处理的条目会在当前安全边界结束，不会强行打断数据库写入。
- 已经完成解析的条目继续保留，等待用户手工确认。
- 尚未开始的条目不会下载媒体或调用 AI；中断不会绕过权限、额度或熔断限制。

关闭应用后，未完成的平台访问不会自动恢复；成功解析和已保存的导入结果仍然保留。

## 4. 固定访问策略

- 固定 3 个异步 worker 负责队列调度和本地工作。
- 短链跳转、F2 单作品解析和公开媒体请求共用一个访问锁，任一时刻只有一个网络请求阶段。
- 每个链接完成或失败后，在锁内随机等待 4–8 秒。
- 每个上海自然日最多发起 150 次公开作品解析；本地视频和外部文件上传不计入此额度。
- 能通过作品 ID 在本地确认已存在的长链接不会访问平台，也不消耗日额度。
- 超时或 5xx 最多重试一次，重试前随机等待 15–30 秒。
- 403、429 或风险验证不重试，并立即终止当前批次的后续访问。
- 熔断会保持 30 分钟并跨应用重启生效，界面没有绕过按钮。

这些限制只能降低触发平台限制的概率，不代表获得官方授权，也不代表零风险。

### 下载权限与处理范围

- 只有接口明确返回 `allow_download=true`，或返回等价且可确认的允许信号，才进入完整媒体下载和音画双轨流程：ASR 保留句级时间戳，画面生成混合候选池，再按视觉需求、OCR、画面描述、清晰度和时间覆盖选择最终关键帧。
- 明确禁止下载时，不保存完整视频、不抽取关键帧；优先读取可用字幕，其次临时下载独立音频做 ASR，最后退化为标题、简介和作者信息。
- 权限字段缺失、类型变化或含义不明确时，按禁止下载处理，不会猜测为允许。
- 开始入库前会刷新一次作品信息；如果作者已关闭下载，会清空链接解析阶段遗留的完整媒体地址并切换到受限流程。
- 独立音频和字幕只作为临时处理输入，处理完成后删除。`music_play_url` 可能只是配乐，无法保证包含完整人声，此时结果会退化为基础信息。

## 5. 结果状态与错误

每个失败结果同时显示稳定错误码和中文说明：

| 错误码 | 含义 |
|---|---|
| `invalid_url` | 未识别到合法链接 |
| `unsupported_host` | 不是支持的公开域名 |
| `redirect_blocked` | 重定向目标不安全或不受支持 |
| `too_many_redirects` | 重定向次数过多 |
| `daily_limit_exceeded` | 达到每日 150 条限制 |
| `access_forbidden` | 抖音拒绝本次解析请求 |
| `rate_limited` | 平台返回 429 |
| `risk_verification` | 出现验证码或风险验证 |
| `network_timeout` | 连接或读取超时 |
| `network_error` | DNS 或其他网络连接失败 |
| `upstream_server_error` | 平台 5xx，重试后仍失败 |
| `unsupported_content_type` | 返回类型不是预期媒体 |
| `f2_cookie_required` | F2 无登录解析失败，可设置可选 Cookie |
| `f2_response_invalid` | F2 未返回有效数据，可能是 Cookie 已失效、平台风控或接口暂不可用 |
| `f2_contract_changed` | F2 返回结构无法识别 |
| `work_unavailable` | 作品不存在、已删除、私密或权限不足 |
| `media_missing` | 元数据可读但没有可处理媒体 |
| `media_expired` | 公开媒体地址失效 |
| `duplicate_input` | 本次输入或其他未完成导入批次里已有同一作品，系统会自动去重 |
| `already_imported` | 本地知识库已存在 |
| `local_file_required` | 可以上传本地文件继续 |
| `cancelled_by_user` | 中断前尚未开始 |
| `security_cleanup_required` | 旧敏感数据尚未清理完成 |

当平台无法给出明确原因时，TokBrain 只显示 `work_unavailable`，不会猜测更具体的删除或权限原因。

## 6. 上传本地补件

允许补件的常见情况包括 `media_missing`、`media_expired`、`f2_cookie_required`、`f2_response_invalid`、`f2_contract_changed` 和 `work_unavailable`。

视频：

- 单个 MP4、MOV、MKV 或 WebM。
- 最大 1 GB。
- 必须包含可识别的视频轨道。

图文：

- 1–12 张 JPEG、PNG 或 WebP。
- 单张最大 30 MB，总计最大 180 MB。

软件忽略客户端文件名，并验证文件实际内容。修改扩展名不能绕过类型检查。源文件会保留至作品被永久删除。

### 与“本地视频导入”的区别

- 本地视频导入用于创建一条全新的本地作品，不要求先有抖音链接或链接导入记录。
- 本地补件用于给已有的链接作品补充有权使用的素材。
- 两者都会执行相同的文件校验和完整音画处理，源文件保留至对应作品被永久删除。

### 高级集成：外部工具批量导入

这仍是面向程序的高级接口。普通用户如果已经拿到下载文件，应使用网页的“外部视频数据包”
选择文件夹或 ZIP；无需制作 JSON manifest、管理 Bearer 令牌或调用推送脚本。

只有在自有工具已经合法取得抖音视频文件和元数据，并需要把最多 100 条记录程序化送入
同机 TokBrain 时，才在“设置 → 外部导入令牌”生成 Bearer 令牌，再按“创建 JSON 清单
→ 逐条上传视频 → 提交批次”调用版本化接口。默认提交后只进入待入库；只有显式设置
`start_processing=true` 才会立即创建 AI 入库任务。

外部接口不接受远程视频 URL、任意本机文件路径或无视频的纯元数据。调用方必须确认有权
处理全部文件；这项声明不能由自定义元数据或 `media_policy` 替代。完整字段、幂等规则、
curl 和 Python 示例见[《外部批量导入 API v1》](docs/external-import-v1.md)。

## 7. 待确认、待入库与检索

链接、本地视频和外部数据包的检测结果都先停在待确认区。用户选择收藏夹并确认后进入
待入库；只有再次主动发起入库，才可能产生媒体时长、Token 和费用。

待入库作品可以稍后单条或批量开始入库。作者明确允许下载或存在本地补件时，才执行完整
媒体、OCR、语音转写、关键帧、总结和向量化；禁止下载或状态未知时，只执行字幕/独立
音频转写或基础信息总结和向量化。处理完成且存在知识块后，作品进入“已入库”；失败会
进入“处理异常”，可按错误码重试。本地视频仍按“上传验证 → 确认加入待入库 → 主动
入库”的文件流程执行。

四种作品状态：

- 待入库
- 已入库
- 处理异常
- 已归档

每个状态的作品卡片都提供“永久删除”。已经生成总结的作品也可以直接删除；操作会一并
清理总结、检索索引和对应本地资产，且不可恢复。总结详情页同样提供永久删除按钮。

新的作品默认加入“手动导入”；应用内链接导入会使用提交前选择的收藏夹，本地视频可在
确认前逐条改选任一已有收藏夹。

### 收藏夹专属总结提示词

1. 在知识库顶部的“本地分组”中选择一个收藏夹。
2. 点击“设置总结提示词”，填写并保存该收藏夹的专属规则。
3. 之后新入库或手动“补齐/更新总结”的作品会使用最近加入的收藏夹提示词。
4. 点击“使用全局提示词”可清除专属规则；未设置专属规则时使用设置页的全局总结提示词。

作品可以属于多个收藏夹。需要重新总结时，以最近一次加入的收藏夹为提示词来源。

## 8. 设置与本地检查

设置页包含：

- 13 套主题与背景强度。
- 固定安全策略的只读说明。
- 数据库、ffmpeg/ffprobe 和敏感残留状态。
- 链接预检、数据包导入和本地处理三个后台协调器的存活 worker 数、期望 worker 数与最近错误。
- 媒体处理额度、AI Token 额度和费用预警。
- F2 可选 Cookie、模型服务密钥和可选账单查询密钥。
- 外部导入令牌的生成、复制、轮换和撤销状态；令牌明文只显示一次。
- 全局视频/图文总结提示词；各收藏夹可在知识库中单独覆盖。

F2 Cookie 默认留空。只有无登录解析不稳定时才需要手动填写；粘贴后必须点击输入框下方
的“保存 Cookie”，并确认页面显示“已保存并生效”。保存一次后，应用内链接导入会自动
复用加密 Cookie，无需每次重新填写。设置页不提供登录窗口、浏览器读取或账号验证。

设置页不提供提高每批、每日、worker 数量、网络并发、冷却时间或熔断时间的入口。

“后台协调器”任一项显示停止时，系统检查会标记为不可用，并展示经过脱敏的最近错误。
先停止并重新启动 TokBrain；若仍未恢复，检查 `data/logs/backend-error.log`，不要通过重复
点击导入或入库来绕过。启动脚本同样会等待三个协调器全部健康后才打开页面。

## 9. 备份与恢复

当前开发版使用数据库结构 schema v9。从旧结构升级时，程序会在 `data/backups/`
生成形如 `douyin_rag-pre-v9-YYYYMMDD-HHMMSS.db` 的时间戳 SQLite 备份，再执行
迁移；从 schema v4 升级会保留已有链接导入与入库队列。

schema v9 会检查受限视频的原始证据。已入库视频若没有本地源文件，下载权限为拒绝或
未知，并且没有字幕、转写、OCR 或视觉描述，就会被标记为“证据不足/需要补件”；迁移还会
清除该条目的正文、总结、知识块和关键帧，避免仅由标题、元数据或生成文本构成的内容继续
参与检索。这不是源备份删除：需要恢复迁移前内容时，先停止 TokBrain，再恢复对应
`pre-v9` 数据库与同一时间点的资产目录。建议同时备份：

- `data/douyin_rag.db`
- `data/source-assets/`
- `data/media/`
- `data/keyframes/`

恢复时先停止 TokBrain，再恢复同一时间点的数据库和资产目录，避免记录与文件不一致。

日常任务不需要手工制作备份代际。处理流程会先把新媒体和关键帧写入暂存代际，并保留旧
代际直到数据库提交成功；提交失败或取消时自动回滚数据库并恢复旧文件。预算预留会释放
未实际使用的部分，已经发生的模型调用仍按真实用量保留，异常退出遗留的预留会在处理
协调器下次启动时回收。若作品显示 `persistence_failed`，检查磁盘空间、数据库和后台
协调器状态后重试；任务运行时不要手工移动或删除 `data/media/`、`data/keyframes/`。

## 10. 常见问题

### 为什么 3 个 worker 看起来仍然很慢？

worker 只并行做本地校验、状态整理和取消检查。平台网络访问必须全局串行，并在每条后等待 4–8 秒。

### 为什么不能立即重试 403、429 或验证页？

这些响应可能代表平台限制。TokBrain 会强制熔断 30 分钟，不提供绕过，避免继续扩大风险。

### 为什么链接导入后知识库里还没有总结？

如果提交时选择“只进入待入库”，系统不会创建 AI 任务；请前往“知识库 → 待入库”开始
入库。请查看任务状态、百炼 API Key、预算和对应错误码。

### 为什么上传文件被拒绝？

检查实际格式、数量和大小，并确认 ffprobe 可用。TokBrain 按文件内容验证，不依据文件名。

### 为什么外部工具返回 401 或 409？

401 通常表示令牌缺失、错误、已撤销或已轮换；请在设置页重新生成并更新调用工具。409
通常表示同一 `Idempotency-Key` 被用于不同清单，或同一条目已经上传了不同文件；重试
同一请求应复用原键和原文件，确需替换尚未提交的资产时显式使用 `replace=true`。

### 为什么重启后批次显示已取消？

这是安全策略。应用不会在无人确认的情况下恢复平台访问；已经成功的结果不会丢失。

### 安装时 npm 报 `EPERM` 或指向 `D:\node_cache` 怎么办？

这通常是电脑上的 npm 全局缓存被设置到了无权限、只读或正被占用的目录。最新版
`安装.cmd` 会为本次安装改用当前用户 `LocalAppData` 下的 TokBrain 专用缓存，不会
修改用户的全局 npm 配置。请重新下载最新版后安装；如果专用缓存仍报 `EPERM`，先
关闭其他 Node.js/npm 进程，并检查安全软件是否拦截该目录。
